<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true" >

  <RelativeLayout
      android:layout_width="fill_parent"
      android:layout_height="wrap_content"
      android:background="#ffffff" >

      <!-- Header Starts -->
      <!-- Header Ends -->


      <!-- Footer Start -->
      <!-- Footer Ends -->


      <!-- Login Form -->
      <!-- Login Form Ends -->

      <LinearLayout
          android:id="@+id/header"
          android:layout_width="fill_parent"
          android:layout_height="wrap_content"
          android:layout_alignParentTop="true"
          android:layout_centerHorizontal="true"
          android:background="#ffffff"
          android:orientation="vertical"
          android:paddingBottom="5dip"
          android:paddingTop="5dip" >
      </LinearLayout>

      <Button
          android:id="@+id/add_new_temperature"
          android:layout_width="200dp"
          android:layout_height="35dp"
          android:layout_above="@+id/back_temperature"
          android:layout_centerHorizontal="true"
          android:text="Add New Temperature"
          android:textSize="15dp" />

      <Button
          android:id="@+id/back_temperature"
          android:layout_width="200dp"
          android:layout_height="35dp"
          android:layout_alignLeft="@+id/add_new_temperature"
          android:layout_alignParentBottom="true"
          android:text="Back"
          android:textSize="15dp" />

      <requestFocus
          android:layout_width="wrap_content"
          android:layout_height="wrap_content" />

      <ListView
          android:id="@+id/view_temperature_date"
          android:layout_width="180dip"
          android:layout_height="wrap_content"
          android:layout_above="@+id/add_new_temperature"
          android:layout_alignParentLeft="true"
          android:layout_below="@+id/textView2"
          android:background="#B8B8B8"
          android:textColor="#100000" >
      </ListView>

      <TextView
          android:id="@+id/textView2"
          android:layout_width="wrap_content"
          android:layout_height="50dp"
          android:layout_alignParentLeft="true"
          android:layout_alignParentTop="true"
          android:layout_alignRight="@+id/view_temperature_date"
          android:gravity="center"
          android:text="Date"
          android:textColor="#000001"
          android:textSize="18sp" />

      <TextView
          android:id="@+id/TextView02"
          android:layout_width="wrap_content"
          android:layout_height="50dp"
          android:layout_alignLeft="@+id/view_temperature_temperature"
          android:layout_alignParentTop="true"
          android:layout_alignRight="@+id/view_temperature_temperature"
          android:gravity="center"
          android:text="Temperature"
          android:textColor="#000001"
          android:textSize="18sp" />

      <ListView
          android:id="@+id/view_temperature_temperature"
          android:layout_width="150dp"
          android:layout_height="wrap_content"
          android:layout_above="@+id/add_new_temperature"
          android:layout_alignParentRight="true"
          android:layout_alignTop="@+id/view_temperature_date"
          android:background="#B8B8B8"
          android:textColor="#100000" >

      </ListView>

  </RelativeLayout>

</ScrollView>